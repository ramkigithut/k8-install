apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: test-cluster-practice-kvkr
  region: us-east-1

managedNodeGroups:
  - name: test25
    spot: true
    ssh:
      publicKeyName: K1